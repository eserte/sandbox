language: perl
matrix:
  include:
    - perl: "5.8"

sudo: true

install:
  - cpanm --quiet --notest IPC::Run Net::OpenSSH BSD::Resource

script:
  - git clone git://github.com/eserte/Doit.git
  - cd Doit
  - perl -Ilib t/file.t

branches:
  except:
    - /appveyor/
