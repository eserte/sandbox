{
  "targets": {
    "xenial-amd64": {
      "buildenv": "xenial-amd64",
      "builddeps": ["perl", "make", "cpanminus"],
      "buildcmd": ["cpanm --quiet --installdeps --notest .", "perl Makefile.PL", "make", "make test"]
    }
  }
}
