{
  "targets": {
    "xenial-amd64": {
      "buildenv": "xenial-amd64",
      "builddeps": ["perl", "make", "cpanminus"],
      "buildcmd": ["cpanm --local-lib=~/perl5 local::lib", "eval $(perl -I ~/perl5/lib/perl5/ -Mlocal::lib) && cpanm --quiet --installdeps --notest . && perl Makefile.PL && make all test"]
    }
  }
}
